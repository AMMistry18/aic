# Repository tools

This directory contains developer-facing utilities that are not part of the
deployed runtime.

- `evaluation/` generates evaluation trials and summarizes scoring results.
- `perception/` contains dataset generation, training, validation, and analysis
  tools grouped by perception target.

Run commands from the repository root. The target-specific READMEs list the
entry points used by local and TACC workflows.
